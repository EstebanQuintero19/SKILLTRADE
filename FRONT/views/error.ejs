
<div class="min-h-screen flex items-center justify-center bg-gray-900 py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full text-center">
        <div class="mb-8">
            <div class="mx-auto h-24 w-24 bg-red-900 rounded-full flex items-center justify-center mb-6">
                <i class="bi bi-exclamation-triangle text-red-400 text-4xl"></i>
            </div>
            <h1 class="text-6xl font-bold text-white mb-4">
                <%= error.status || 500 %>
            </h1>
            <h2 class="text-2xl font-semibold text-gray-300 mb-4">
                <%= title || 'Error' %>
            </h2>
            <p class="text-gray-400 mb-8">
                <%= message || 'Ha ocurrido un error inesperado' %>
            </p>
        </div>
        
        <div class="space-y-4">
            <a href="/" class="inline-block bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors">
                <i class="bi bi-house mr-2"></i>
                Volver al Inicio
            </a>
            <div>
                <button onclick="history.back()" class="text-gray-400 hover:text-white transition-colors">
                    <i class="bi bi-arrow-left mr-2"></i>
                    Página Anterior
                </button>
            </div>
        </div>
    </div>
</div>
